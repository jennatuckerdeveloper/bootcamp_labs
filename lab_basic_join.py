#join method of strings
a = "-";
#this is the string separator, the thing between the strings to be joined
#this does not work without the ;
seq = ("heart", "eyes")
#strings to join put into one variable.  why isn't this on []?
#I guess instead of a list / array, it's a sequence of strings / variables
print(a.join(seq))
#separator variable, join function, parameter is variable holding strings
