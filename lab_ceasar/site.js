
var test = "this is my string to encrypt";

var plain = "abcdefghijklmnopqrstuvwxyz";

function ceasar(plainStr, key) {
    place =
    for (var i = 0, len = plainStr.length; i < len; i++) {
        alert(str[i]);
}
}


caesarEncrypt(plainStr, key);
caesarDecrypt(encStr, key);
